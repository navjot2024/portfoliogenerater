# Frontend env example — safe to commit
# REACT_APP_API_URL should point to your backend's base URL (no trailing slash)
# Example for local dev when running backend on localhost:5000:
REACT_APP_API_URL=http://localhost:5000/api

# Example for production (set this in Render or your host):
# REACT_APP_API_URL=https://portfoliogenerater-backend.onrender.com/api
